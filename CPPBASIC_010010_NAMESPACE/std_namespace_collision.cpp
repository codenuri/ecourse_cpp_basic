#include <algorithm>
using namespace std;

int count = 0;

int main()
{
//	int n1 = std::max(3, 1);
//	int n2 = std::min(3, 1);

	int n1 = max(3, 1);
	int n2 = min(3, 1);

	count = 10;
}

